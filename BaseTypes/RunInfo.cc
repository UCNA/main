#include "RunInfo.hh"

/*												 
void RunInfo::specialize() {
	
	// bad GMS ref pmt data alignment for these runs
	if(7234 <= runNum && runNum <= 7300)
		special["Scrambled GMS"] = 1;
	
	// W GMS LED not seen by 4 tubes
	if(7300 < runNum && runNum < 7354)
		special["Broken W GMS"] = 1;
	
	// ref gain run setting
	if(runNum < 7000)
		special["Ref Gain Run"] = 6723;
	else if(runNum < 7600)
		special["Ref Gain Run"] = 7320;
	else
		special["Ref Gain Run"] = 7676;
	
	// linearity repairs settings
	if(7000 < runNum)
		special["No Linearity Repairs"] = 1;
	
	
	// run quality reference
	qualRefRun = 0;
	
}
*/
